bin/
obj/
_preview/
/dist
# WPF markup-compilation scratch projects (auto-generated in the project dir during XAML build,
# normally deleted afterward — ignored so a stray one is never tracked/committed).
*_wpftmp.csproj
.roadkeep/
/scripts/demo
TestResults/

# WW315. The two profiles and the transcript a run fabricates for itself. Built where the cases
# run and deleted after them, so it never travels to a guest and never outlives the run that made it.
/bench/
